# Aetheris Oracle - Dependencies
# Install with: pip install -r requirements.txt

# Core ML/Scientific
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
pandas>=2.0.0

# Web Framework
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
httpx>=0.24.0

# Data Connectors (No API keys needed!)
ccxt>=4.0.0
yfinance>=0.2.28

# Configuration
python-dotenv>=1.0.0

# Visualization
matplotlib>=3.7.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Progress bars
tqdm>=4.65.0

# Neural SDE / Differential Equations
torchdiffeq>=0.2.3

# Tensor operations
einops>=0.6.0

# Gaussian Processes (for FM-GP)
gpytorch>=1.11

# Optional: Experiment tracking (uncomment if needed)
# wandb>=0.15.0
# mlflow>=2.10.0

# Optional: MambaTS state-space models
# Requires CUDA and may not install on all systems
# mamba-ssm>=1.2.0
ast-grep-py
pyarrow
# Editable install (for development)
-e .
